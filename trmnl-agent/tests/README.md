# TRMNL Agent Test Suite

## Overview

This test suite provides comprehensive coverage for the TRMNL Agent application with a primary focus on **mocking networking calls** using `pytest` and `pytest-mock`. All HTTP requests are intercepted and mocked to ensure tests run reliably without external dependencies.

